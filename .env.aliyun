# 阿里云部署环境配置
# 在阿里云服务器上执行：source .env.aliyun && npm run dev

# API 后端地址（阿里云公网 IP）
export VITE_API_URL="http://8.130.148.166:8000"

# 服务端口
export PORT=3000

# 部署路径
export DEPLOY_DIR="/opt/behavior-recorder/behavior_recorder_service"
