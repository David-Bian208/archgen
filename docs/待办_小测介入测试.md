# 待办：小测介入测试

**优先级：** P0  
**分配给：** 小测  
**时间：** 2026-04-08 11:53  
**前置条件：** ✅ 小治已完成前端 API 代理修复

---

## 测试背景

**项目：** 行为观察助手 V4.11.0  
**当前状态：** 开发完成，前端 API 代理已修复，等待功能验证  
**测试类型：** 功能测试 + 集成测试

---

## 测试范围

### 1. 核心功能测试

**测试 API 端点：** `/api/v4/analyze`

**测试用例（优先级排序）：**

| 编号 | 场景 | 输入 | 预期输出 |
|------|------|------|----------|
| T01 | 社交逃避 | "叫他不理，不看人" | 识别 H_ESCAPE_DIFFICULTY |
| T02 | 寻求关注 | "故意捣乱引起注意" | 识别 H_ATTENTION_SEEKING |
| T03 | 坚持同一性 | "路线变了就崩溃" | 识别 H_RIGIDITY |
| T04 | 感官敏感 | "听到声音就捂耳朵" | 识别 H_SENSORY |
| T05 | 危险行为 | "打人、咬人、发火" | 安全优先模式触发 |

### 2. 前端功能测试

**测试页面：** `http://localhost:5173`

| 编号 | 测试项 | 操作步骤 | 预期结果 |
|------|--------|----------|----------|
| F01 | 页面加载 | 打开首页 | 无报错，表单正常显示 |
| F02 | 输入验证 | 空提交 | 提示输入内容 |
| F03 | API 调用 | 输入短文提交 | 无 CORS 错误，收到响应 |
| F04 | Loading 提示 | 提交后 | 显示"预计 5-10 秒" |
| F05 | 报告展示 | 收到响应后 | 正确渲染分析报告 |
| F06 | 错误处理 | 断网提交 | 显示友好错误提示 |

### 3. 性能测试

**测试脚本：** `tests/concurrent_test.py`

**预期指标：**
- 单次响应时间：<10s
- 10 并发成功率：>90%
- 无内存泄漏

---

## 执行步骤

### Step 1: 环境验证

```bash
# 1. 确认后端服务
curl http://localhost:8001/health
# 预期：{"status":"ok","version":"V4.11.0"}

# 2. 确认前端服务
curl http://localhost:5173 | head -3
# 预期：返回 HTML

# 3. 确认 API 可用
curl -X POST http://localhost:8001/api/v4/analyze \
  -H "Content-Type: application/json" \
  -d '{"user_input":"小明今天很开心"}'
# 预期：返回有效 JSON 响应
```

### Step 2: 执行核心功能测试

```bash
cd /home/admin/.openclaw/workspace/behavior_recorder_service

# 运行测试脚本（如果有）
python3 tests/test_core_functionality.py

# 或手动测试
python3 tests/manual_test_api.py
```

### Step 3: 执行前端测试

```bash
# 使用 Playwright 或 Selenium（如果有自动化测试）
cd frontend
npm run test:e2e

# 或手动浏览器测试
# 打开 http://localhost:5173 手动操作
```

### Step 4: 执行性能测试

```bash
cd /home/admin/.openclaw/workspace/behavior_recorder_service
python3 tests/concurrent_test.py
```

---

## 通过标准

**P0（必须通过）：**
- [ ] 所有核心功能测试用例通过
- [ ] 前端 API 调用无 CORS 错误
- [ ] 安全优先模式正确触发

**P1（应该通过）：**
- [ ] 前端 UI 正常渲染
- [ ] Loading 提示正确显示
- [ ] 错误处理友好

**P2（最好通过）：**
- [ ] 性能指标达标
- [ ] 无内存泄漏

---

## 输出要求

**测试完成后提交：**

1. **测试报告文件：** `docs/TEST_REPORT_2026-04-08.md`
   ```markdown
   # 测试报告
   
   ## 执行概况
   - 测试用例数：X
   - 通过数：Y
   - 失败数：Z
   - 通过率：Y/X%
   
   ## 详细结果
   | 用例 | 状态 | 备注 |
   |------|------|------|
   | T01 | ✅ | - |
   | T02 | ❌ | 具体错误信息 |
   
   ## 已知问题
   1. 问题描述 + 复现步骤
   
   ## 建议
   1. 修复建议
   ```

2. **Git 提交测试记录**
   ```bash
   git add docs/TEST_REPORT_2026-04-08.md
   git commit -m "test: V4.11.0 功能测试报告"
   ```

3. **通知战舰** - 测试完成，报告已生成

---

## 相关文件

- `docs/待办_前端 API 代理修复.md` - 小治修复记录
- `docs/TEST_CASES.md` - 完整测试用例（如有）
- `docs/KNOWN_ISSUES.md` - 已知问题（更新用）
- `tests/` - 测试脚本目录

---

## 联系方式

**测试遇到问题时：**
- 查看 `docs/待办_前端 API 代理修复.md` 了解修复内容
- 检查后端日志：`logs/api.log`
- 检查前端日志：`logs/frontend.log`

---

**测试完成后通知：** @战舰
