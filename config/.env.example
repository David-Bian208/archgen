# ArchGen 环境变量模板
# 复制为 config/.env 并填入实际值

# DeepSeek API 密钥（必填）
DEEPSEEK_API_KEY=sk-xxxxx

# ArchGen 服务端口（可选，默认 8972）
# ARCHGEN_PORT=8972

# ArchGen API 鉴权密钥（可选，留空则不启用 API 鉴权）
# ARCHGEN_API_KEY=

# AI Pulse 服务地址（可选）
# AI_PULSE_HOST=
