# 待办：前端展示修复（叙事分析 + 干预建议验证）

**优先级：** P0  
**分配给：** TRAE（小治）  
**时间：** 2026-04-08 12:17  
**问题来源：** DAVID 用户反馈

---

## 问题描述

### 问题 1：叙事分析前端没显示 ❌

**现象：**
- 阿里云部署版本后端返回了"叙事分析"部分
- 但前端页面没有显示这部分内容
- 用户看不到完整的分析报告

**原因：**
- 后端 `structured_assessor_v4.py` 已生成 `narrative_analysis`
- 前端 `App.vue` 没有渲染这个字段

**后端已有数据：**
```python
# structured_assessor_v4.py line 954
"narrative_analysis": narrative,
```

**叙事分析内容示例（来自阿里云）：**
```
叙事分析:
在换位思考任务中，儿童表现出对他人视角理解的困难。具体表现为:
1) 在"错误信念"任务中，当被问及他人 (妈妈) 会看到什么时，儿童无法理解他人持有与自己不同的信念...
2) 在"空间视角转换"任务中...
最可能的整合性归因是儿童在"心理理论"发展方面存在阶段性延迟...
```

---

### 问题 2：干预建议升级验证 ⚠️

**用户反馈：**
> "我们本次花了那么多时间的升级，结果关键部分，也就是干预建议这块的升级什么都没做"

**需要验证：**
1. 后端 `intervention_planner.py` 是否真的升级了 V4.10.4 的干预逻辑？
2. 干预建议是否包含场景化映射？
3. 是否包含能力缺口同义词库匹配？

**检查文件：**
- `app/agents/intervention_planner.py`
- `app/knowledge/intervention_scene_mapping.md` (场景映射库)

---

## 修复任务

### 任务 1：前端添加叙事分析展示

**文件：** `frontend/App.vue`

**修改位置：** 在"临床鉴别思考"和"干预建议"之间添加

**参考代码：**
```vue
<!-- 叙事分析 -->
<div v-if="insightReport.narrative_analysis" class="narrative-section">
  <h3>📖 叙事分析</h3>
  <div class="narrative-content" v-html="formatNarrative(insightReport.narrative_analysis)"></div>
</div>
```

**需要添加：**
1. HTML 模板部分 - 添加叙事分析区块
2. 数据解析 - 确保 `insightReport.narrative_analysis` 被正确赋值
3. 格式化方法 - `formatNarrative()` 处理换行和加粗

**样式参考：**
```css
.narrative-section {
  background: #f8f9fa;
  padding: 20px;
  border-radius: 8px;
  margin: 20px 0;
  border-left: 4px solid #667eea;
}

.narrative-content {
  line-height: 1.8;
  color: #333;
}
```

---

### 任务 2：验证干预建议升级

**检查清单：**

```bash
# 1. 检查 intervention_planner.py 是否有场景分类
grep -n "scene_classification\|场景分类" app/agents/intervention_planner.py

# 2. 检查是否有能力缺口同义词库
grep -n "capability_keywords\|同义词" app/agents/intervention_planner.py

# 3. 检查场景映射库是否存在
ls -la app/knowledge/intervention_scene_mapping.md

# 4. 检查 V6.0 能力缺口匹配逻辑
grep -n "_match_capability_keywords\|V6.0" app/agents/intervention_planner.py
```

**预期结果：**
- ✅ 场景分类引擎（5 类场景识别）
- ✅ 能力缺口同义词库（92 个同义词）
- ✅ 场景→干预映射库
- ✅ `_match_capability_keywords()` 方法

**如果没有这些代码：**
- 说明干预建议部分确实没有升级
- 需要从备份或文档中恢复 V6.0 的代码

---

### 任务 3：测试验证

**修复后测试：**

1. **叙事展示测试**
   ```bash
   # 提交测试输入
   curl -X POST http://localhost:8001/api/v4/analyze \
     -H "Content-Type: application/json" \
     -d '{"user_input":"叫他不理，不看人"}'
   
   # 检查响应中是否有 narrative_analysis
   ```

2. **前端展示测试**
   - 打开 `http://localhost:5173`
   - 提交测试
   - 确认"📖 叙事分析"区块显示
   - 确认内容格式正确（换行、加粗）

3. **干预建议测试**
   - 确认干预建议与场景匹配
   - 确认能力缺口描述准确

---

## 验收标准

### 叙事分析展示
- [ ] 前端显示"📖 叙事分析"区块
- [ ] 内容完整（200-300 字）
- [ ] 格式正确（换行、加粗、列表）
- [ ] 位置正确（临床鉴别思考后，干预建议前）

### 干预建议验证
- [ ] 确认代码包含场景分类引擎
- [ ] 确认代码包含能力缺口同义词库
- [ ] 测试用例验证匹配准确

---

## 相关文件

- `frontend/App.vue` - 前端主组件
- `app/agents/structured_assessor_v4.py` - 后端报告生成
- `app/agents/intervention_planner.py` - 干预建议生成
- `app/knowledge/intervention_scene_mapping.md` - 场景映射库

---

## 参考文档

- V4.10.4 报告对比分析：`/home/admin/.openclaw/workspace/V4.6.6_报告对比分析与优化指令.md`
- V6.0 能力缺口匹配：`behavior_recorder_service/V6.0_能力缺口匹配逻辑优化报告.md`

---

**完成后通知：** @战舰  
**优先级：** P0（用户直接反馈）
