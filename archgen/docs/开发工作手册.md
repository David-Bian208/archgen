# ArchGen 开发工作手册

> 版本：v1.1 | 更新时间：2026-07-05 | 开发者：TRAE / 战舰 / 小治

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [核心工作流](#核心工作流)
4. [P0-P3 功能总览](#p0-p3-功能总览)
5. [素材匹配机制](#素材匹配机制)
6. [返回上一步规则](#返回上一步规则)
7. [常见问题排查](#常见问题排查)
8. [代码规范](#代码规范)

---

## 项目概述

**定位：** 本地运行的智能分析写作工作台，基于 MCP 文件直读架构

**核心架构（5 步流水线）：**
1. **选题** - 用户输入创作主题
2. **补充** - 知识库 / AIPULSE / 联网搜索 / 手动补充
3. **三列分析工作台** - AI 流式推理框架 → 素材匹配 → 提纲生成
4. **文章生成** - AI 基于确认内容自动生成全文
5. **配图生成** - Playwright + HTML 模板生成架构图

**核心原则：**
- 零向量库（不搞 embedding、不搞切块）
- 成本敏感（任何优化不增加 LLM 调用次数）
- 用户感知优先（任何网络 IO 不阻塞用户交互路径）

**项目路径：** `/home/admin/.openclaw/workspace/behavior_recorder_service/archgen/`

**访问地址：**
- 后端 API：`http://localhost:8951`
- 前端：`http://localhost:3014`

**运行依赖：**
- Python >= 3.10
- Node.js >= 18
- Playwright Chromium（`playwright install chromium`）

---

## 技术栈

### 前端
- Vue 3.3+ + Vite 4.4+
- Arco Design Vue 2.5+
- 工具库：dayjs、axios

### 后端
- Python ≥3.10
- FastAPI 0.100+ + Uvicorn 0.23+
- SQLite 3.37+

---

## 核心工作流

### 5 步概览

```
选题 → 补充/方向选择 → 三列分析工作台 → 文章生成 → 配图生成
 ↑0        ↑1             ↑2                ↑3          ↑4
```

### Step 0：选题
- 用户直接输入创作主题
- 或通过 MCP 检索本地知识库获取主题灵感
- **输出**：选题方向 + 初始素材摘要

### Step 1：补充 / 方向选择
- **角度推荐**：AI 基于素材分析推荐 3-5 个写作角度（含素材覆盖度）
- **补充入口**：AIPULSE 预拉取 / 联网搜索 / 手动补充 / AI 推断补充
- **P3 改动**：确认方向时后台异步拉取 AIPULSE（`days=30, take=20`）
- **输出**：确认写作方向 + 素材池（含 AIPULSE 结果）

### Step 2：三列分析工作台（核心）
```
Phase 1: 流式推理 → AI 输出分析框架（5-8 个槽位）→ 用户可编辑
Phase 2: 批量填充 → 素材匹配（label+desc+search_keywords）→ 提纲生成
Phase 3: 确认 → 三列表格展示（槽位/素材/提纲）→ 逐行确认
```

**P3 关键特性**：
- 第一轮匹配（label+desc）→ LLM 生成提纲 + search_keywords（同一调用）
- 第二轮匹配（label+desc+search_keywords）→ 返回更精准的素材
- L3 联网兜底开关：开启后用 DuckDuckGo 搜索补充素材

**B/D 优化**：
- 素材显示匹配片段（~80 字）代替全文预览
- 每项显示匹配得分（N 分）和去重标记（⏺️ 相似）
- 表格上方汇总条：🟢 N 个充足 🟡 N 个偏少 🔴 N 个为空

### Step 3：文章生成
- **输入**：确认后的三列内容（槽位名 + 提纲 + 素材）
- **输出**：完整 Markdown 文章
- **字数控制**：可设目标字数（2000/3000/5000/自定义）

### Step 4：文生图（信息图）v4.0

**输入**：完整文章 Markdown + framework_name（两条路径：Archgen 槽位 / 外部文章）
**输出**：HTML → 前端 iframe 预览
**核心变更（v3.0 → v4.0）**：LLM 从"内容+排版全包"收敛为"只做内容拆解"，排版和渲染全部由代码规则和 Cherry 精确参数接管。

#### 架构：三站接力

```
POST /api/generate/infographic
  │
  ▼
┌──────────────────────────────────────────────┐
│  第零站：定画布（代码写死）                    │
│  · 竖版 1080×1440，Hero 280px + 5行×232px    │
│  · 横版 1920×1080，Hero 240px + 4行×210px    │
└──────────────────┬───────────────────────────┘
                   ▼
┌──────────────────────────────────────────────┐
│  第一站：拆卡片（LLM 做）                     │
│  · 函数: stage1_extract_prompt_v4()          │
│  · 输入: 文章内容                            │
│  · 输出: N 张信息卡片 JSON                    │
│  · 每张含: title, items, density,            │
│    content_shape, semantic_weight,           │
│    icon, badge, relation                     │
│  · LLM 只做内容拆解，不做排版决策             │
│  · temperature=0.4, max_tokens=4096          │
│  · 两条路径统一输出为相同卡片 JSON 格式        │
└──────────────────┬───────────────────────────┘
                   ▼
┌──────────────────────────────────────────────┐
│  第二站：排格子（代码规则 bin_packer.py）      │
│  · 函数: pack_cards_into_rows()              │
│  · density + content_shape → 行列分配        │
│  · 三重规则:                                 │
│    1. flow/grid → 强制独占整行               │
│    2. density=high → 强制独占整行            │
│    3. 其他 → 拼车候选                         │
│  · 拼车约束：竖版最多2列，横版最多3列          │
│  · 溢出：合并相邻 low list 卡片，不删内容      │
└──────────────────┬───────────────────────────┘
                   ▼
┌──────────────────────────────────────────────┐
│  第三站：印海报（代码模板 cherry_fidelity.py） │
│  · content_shape → 选渲染模板                 │
│  · highlight / list_solo / list_half          │
│  · grid / flow / compare                     │
│  · 所有颜色/字号/间距写死在代码里，精确到像素   │
└──────────────────┬───────────────────────────┘
                   ▼
             返回 HTML → 前端 iframe 预览
```

任一阶段失败 → 降级到单次 CoT（采用 compact 默认参数，LLM 直接输出完整 HTML）。

#### 格式预设

| 预设 | 尺寸 | 比例 |
|------|------|------|
| portrait | 1080×1440 | 3:4 竖版 |
| landscape | 1920×1080 | 16:9 横版 |
| custom | 自定义宽高 | 宽高比决定参数 |

#### 卡片字段（LLM 第一站输出）

| 字段 | 类型 | 必须 | 说明 |
|------|------|:--:|------|
| title | string | ✓ | 卡片标题，≤12字 |
| items | array | ✓ | 要点列表，2-5条 |
| items[].bold | string | | 加粗前缀关键词 |
| items[].text | string | ✓ | 要点正文 |
| density | enum | ✓ | high/medium/low |
| content_shape | enum | ✓ | flow/grid/list/compare |
| semantic_weight | enum | ✓ | primary/secondary/supporting |
| icon | string | | 固定图标库选（20-30个），不可自创 |
| badge | string | | 原文量化亮点 |
| relation | object | | 可选，pair/contrast 关联 |

#### content_shape 视觉结构

| 值 | 含义 | 装箱处理 |
|------|------|------|
| flow | 流程型，节点+箭头 | 强制独占整行 |
| grid | 网格型，3-4列全宽 | 强制独占整行 |
| list | 列表型，要点罗列 | 可拼车（低密度→2-3列） |
| compare | 对照型，左右对照 | 可拼车 |

#### 装箱规则（bin_packer.py）

```
规则1: content_shape ∈ {flow, grid} → 独占整行
规则2: density == "high" → 独占整行
规则3: 拼车候选：
  竖版: list/compare + density=low → 2列
  横版: list + density=low → 3列; +medium → 2列
         compare + density∈{low,medium} → 2列
  其他 → 1列
溢出: 合并相邻 low list 卡片（合并title+items，打 medium 标签）
绝不删内容，只合并。合并后还超 → JS 自缩放兜底。
```

#### 渲染模板（cherry_fidelity.py）

| 模板 | 匹配条件 | HTML 结构 |
|------|------|------|
| highlight | density=high + 有 badge | 渐变背景 + 左要点 + 右徽章 |
| list_solo | content_shape=list + 独占 | 白底卡片 + 图标列表 |
| list_half | content_shape=list + 双列 | 同上，width:50% |
| grid | content_shape=grid | 3列灰格，图标+标题+描述 |
| flow | content_shape=flow | 横排图标→箭头→图标→箭头 |
| compare | content_shape=compare | 左右对照双列 |

#### 图标系统

固定图标库（20-30个）：architecture / rocket / shield / chart / workflow / code / cloud / database / team / target / lightbulb / scale / clock / puzzle / gear / star / flag / layers / compare / network / file / search / check / robot / filter / download / book / newspaper / calendar

LLM 从库中选择，不允许自创新名。未匹配 → 默认 `file`。

#### 颜色系统

| 颜色 | 色值 | 使用位置 |
|------|------|----------|
| primary | #165DFF | 编号圆、标题、图标 |
| secondary | #36CFFB | 渐变端点 |
| text_main | #374151 | 正文要点 |
| text_title | #1F2937 | 模块标题 |
| text_sub | #4B5563 | 辅助说明 |
| card_bg | #FFFFFF | 卡片背景 |
| page_bg | #F9FAFB | 页面背景 |
| border | #F3F4F6 | 卡片边框 |

#### 字号阶梯（1080px 画布基准）

| 层级 | 字号 | 适用范围 |
|------|:--:|------|
| 头部标题 | 24px | Hero 区标题 |
| 模块标题 | 27px | 每行 h2 标题 |
| 编号圆 | 19px | 序号圆圈内文字 |
| 高亮正文 | 22px | highlight 型卡片要点 |
| 普通正文 | 19px | list 型卡片要点 |
| 小说明文 | 17px | grid 型描述文字 |
| 底栏 | 19px | footer 区文字 |

#### 判定机制（三层）

```
第一层：规则推导（确定性，零 LLM 成本）
  关键词检测 → 初步 shape
  含"步骤/流程/第一第二" → flow
  含"vs/相比/对比/差异" → compare
  描述字数 > 标题×2 → grid
  其他 → list

第二层：LLM 标注（覆盖模糊情况）
  LLM 显式输出 density / content_shape

第三层：规则兜底（安全网）
  LLM 说 flow 但只有 2 项且无顺序词 → 降级 list
  LLM 说 grid 但只有 2 项 → 降级 list
```

#### 关键文件

| 功能 | 文件 | 说明 |
|------|------|------|
| API 端点 | `api.py` → `generate_infographic_v4()` | 入口 |
| LLM 拆解提示词 | `stage1_extract_prompt_v4.py` | 第一站：文章→卡片 JSON |
| 装箱引擎 | `bin_packer.py` | 第二站：卡片→行列分配 |
| 渲染引擎 | `cherry_fidelity.py` | 第三站：行列→HTML（含固定图标库） |
| 详细规范 | `docs/IMAGE_GENERATION_SPEC.md` v4.0 | — |

#### 待实现

- **P0**: `stage1_extract_prompt_v4.py` — 新提示词文件
- **P0**: `bin_packer.py` — 装箱引擎
- **P0**: `cherry_fidelity.py` — 渲染引擎
- **P1**: 人工确认界面（可调整卡片拆分/合并）
- **P2**: JS 自缩放兜底（溢出保护）

---

## Skill 使用指南

### 已注册 Skill

| Skill 名称 | 唤醒方式 | 用途 |
|-----------|----------|------|
| `frontend-button-debug` | `@frontend-button-debug` | 前端按钮调试 |

### 使用示例

#### 场景 1: 用户反馈按钮点击无效

**错误做法：**
```
小治，检查一下"返回补充"按钮为什么无效
```

**正确做法：**
```
@frontend-button-debug 用户反馈"返回补充"按钮点击无效，请按流程调试
```

**Skill 会自动执行：**
1. 添加调试日志
2. 检查 UI 遮挡（`<a-spin>`、v-if、disabled）
3. 检查事件绑定
4. 检查函数逻辑
5. 生成用户验证指南

---

---

## 素材分析（原 AI 问答 — 待改造）

### 现状（问题）

编辑面板（EditPanel）底部有「💬 追问 AI」功能，允许用户输入自由文本提问。**当前是坏的**：

| 问题 | 原因 | 表现 |
|------|------|------|
| LLM 无素材上下文 | prompt 只传了话题名 + MCP 摘要（500 字），没传槽位的实际素材和提纲 | 回答都是"请提供具体素材"——但它看不到素材 |
| 无产出闭环 | AI 答完就完了，答案既不能保存也不能应用到提纲 | 用户不问就永远空着 |

### 业务决策

**改造方向**：从「自由问答」改为「任务驱动分析」，让用户一键获得可落地的分析结果。

**不改其他地方**：素材和提纲同时存在的唯一场景是 Step 2 编辑面板。其他步骤（Step 0/1/3/4）没有分析需求。

### 改造方案

**交互变化**：

```
改造前                             改造后
┌─ 💬 追问 AI ──────────┐        ┌─ 🔍 素材分析 ──────────┐
│                        │        │                         │
│ [输入框] 发送          │        │ 📊 总结核心观点          │
│                        │        │ ↔ 与提纲关联分析         │
│ 你问：这个素材说了什么？│        │ ✏️ 扩写这个提纲           │
│ AI答：请提供素材...     │        │ 🔗 推荐相关方向           │
│                        │        │                         │
└────────────────────────┘        │ ─── 分析结果 ───         │
                                  │ (自动展示最近一次分析)    │
                                  │                         │
                                  │ [✅ 应用到提纲]          │
                                  └─────────────────────────┘
```

**后端新增 1 个端点：**

```
POST /api/workflow/slot/analyze_material
参数: { session_id, slot_key, action_type }
  action_type:
    - summarize     → 总结素材核心观点
    - relevance     → 分析素材与提纲的关联
    - expand        → 基于素材扩写当前提纲
    - recommend     → 推荐补充方向

返回: { analysis, suggested_outline_update }
```

**关键设计：**
- 分析结果含 `suggested_outline_update` 字段（可直接更新的提纲内容）
- 用户点击「应用到提纲」时自动更新该槽位的提纲行
- 每次点击按钮重新触发 AI 分析（结果不缓存）
- 自由输入框改为按钮组——用户不需要想"该问什么"

### 改动范围

| 层 | 改动量 | 说明 |
|----|--------|------|
| 后端 | ~1 个新端点（可复用 `ask_followup` 的逻辑） | `api.py` +30~50 行 |
| 前端编辑面板 | 替换 UI + 对接新端点 | `EditPanel.vue` |
| 前端工作台 | 新增「应用到提纲」事件处理 | `ThreeColumnWorkbench.vue` |

### 不做的设计

- ❌ 不保留自由输入框（用户不会用，且需要额外 prompt 安全处理）
- ❌ 不做多轮对话（每轮分析独立，减少上下文混乱）
- ❌ 不缓存分析结果（素材确认后一般不重新分析，缓存价值低）
- ❌ 不改 Step 3/4（素材分析的上下文只在 Step 2 存在）

---

## P0-P3 功能总览

| 阶段 | 功能 | 状态 |
|------|------|------|
| P0 | 5 步工作流 UI、AI 流式推理框架、六维人设配置 | ✅ |
| P1 | AI-Pulse 接入、降级链（L0-L4）、source_tag 约束 | ✅ |
| P2 | 长素材自动压缩（超过阈值自动 LLM 摘要） | ✅ |
| P3 | AIPULSE 预拉取、search_keywords 二次匹配、L3 联网兜底 | ✅ |
| B/D | 匹配片段预览、得分可视化、去重标记、覆盖度汇总条 | ✅ |
| H1 | 素材来源统计条（表格上方显示各来源条数） | ✅ |
| H2 | AIPULSE 进度提示（Step 1 确认方向后轮询展示拉取进度） | ✅ |
| H3 | 素材分析改造（AI 问答 → 4 任务按钮 + 应用到提纲） | ✅ |
| M1 | L3 空槽位提示按钮 | ✅ |
| M2 | 文章生成并行化（多段落并行生成） | ✅（已实现，仅修复超时） |

## 返回上一步规则

| 当前位置 | 点"返回上一步" |
|---------|---------------|
| Step 4 配图页 | Step 3 文章页 |
| Step 3 文章页 | Step 2 done（保留全部确认状态） |
| Step 2 filling / done / editing_slots | Step 1（方向选择页） |
| Step 2 streaming（流生成中） | 停止流 → Step 1 方向选择页 |
| Step 1 | Step 0 选题页 |
| Step 0 | 不动 |

## 素材匹配机制

### 核心算法：双字词 + 整词 + 单字三重评分

```python
def _match_materials_internal(session_id, confirmed_slots, search_keywords=None):
    # 1. 构造匹配文本 = label + description + search_keywords
    # 2. 三重评分：
    #    a) 整词匹配（权重: 字数×3）
    #    b) 双字词/bigram匹配（权重: 每个4分）
    #    c) 单字重叠度（阈值0.25，权重: overlap_ratio × 15）
    # 3. 按 score 降序排列
    # 4. 去重标记：200字前缀 SequenceMatcher > 0.8 → is_duplicate=true
    # 5. 返回每项含：text, source_type, filename, score, match_snippet, is_duplicate
```

### 素材池来源（3 层）

| 优先级 | 来源 | source_type | 获取时机 |
|--------|------|-------------|---------|
| 1 | MCP 知识库 (KB) | `knowledge_base` / `mcp_summary` | Step 1 方向选择时 |
| 2 | AIPULSE 资讯 | `aipulse` | Step 1 方向确认后后台预拉取 |
| 3 | DuckDuckGo 联网 | `web_search` | Step 2 用户开启 L3 开关时 |

### P3 改进点

1. **AIPULSE 预拉取**：方向确认时后台异步拉 20 条（`time_filter=month`），不阻塞主线
2. **search_keywords**：LLM 生成提纲时同一调用输出 3-5 个搜索关键词，二次匹配提高 recall
3. **去重**：title 相似度（SequenceMatcher > 0.6）+ 文本前缀 200 字符
4. **B/D 优化**：match_snippet 片段提取 + is_duplicate 标记 + 得分可视化 + 覆盖度汇总条

### H1-H2 本期新增（2026-07-05）

#### H1：素材来源统计条

表格上方显示素材池各来源条数（纯前端 computed，数据已存在）：

```
素材池：📄 本地 KB 5 条 | 📡 AI-Pulse 13 条 | 🌐 联网搜索 0 条
```

遍历 `confirmed_slots[].matched_materials[].source_type` 分组计数。

#### H2：AIPULSE 进度提示

**后端新增端点**：`GET /api/workflow/aipulse_status/{session_id}`

返回：
```json
{
  "status": "fetching" | "done" | "failed" | "empty" | "idle",
  "count": 13,
  "error": null
}
```

**前端**：Step 1 确认方向后启动 2 秒间隔轮询，Step 2 顶部展示进度条。

#### H3：素材分析改造（2026-07-05）

**后端新增端点**：`POST /api/workflow/slot/analyze`

EditPanel 底部从自由问答输入框改为 4 个任务按钮：
- 📊 核心观点 — 从素材中提炼核心观点
- 📈 提纲关联 — 分析素材与提纲的关联
- ✏️ 扩写提纲 — 根据素材扩写提纲要点
- 🔗 扩展方向 — 提出延伸讨论方向

分析结果可直接「应用到当前提纲」。

#### M1：空槽位提示

无素材的槽位在展开时显示 `💡 开启联网兜底` 按钮，点击自动触发联网搜索并重新匹配。

#### M2：文章生成并行化

已有实现：Stage 1 使用 `asyncio.gather` 并行生成各段落草稿，Stage 2 润色拼接全文。仅修复超时时间从 30s → 120s。

---

## 常见问题排查

### 问题 1: 按钮点击无效

**排查流程：**
1. 打开浏览器控制台（F12）
2. 点击问题按钮
3. 观察是否有 🔴 [DEBUG] 日志
4. 观察是否有错误信息
5. 反馈截图给开发者

**常见根因：**
- `<a-spin>` 遮罩层覆盖
- v-if 条件不满足（按钮不在 DOM 中）
- disabled 状态卡住
- 事件被阻止（stopPropagation）

### 问题 2: 页面切换时报错

**错误信息：** `Cannot read properties of null (reading 'type')`

**根因：** Arco Design `<a-spin>` 与 Vue 响应式系统冲突

**修复方案：** 见 `frontend/重构方案.md`

### 问题 3: 进度条不显示当前阶段

**排查：** 检查 `currentStep` 值是否正确

**调试：**
```javascript
console.log('currentStep:', currentStep.value)
```

---

## 代码规范

### 前端规范

#### Loading 状态管理（重要）

所有涉及 `loading.value` + `currentStep` 切换的异步函数，必须遵循：

```javascript
async function someStep() {
  loading.value = true
  try {
    const res = await someApi()
    
    if (res.data.code === 0) {
      data.value = res.data.data
      
      // ✅ 先关闭 loading
      loading.value = false
      
      // ✅ 延迟切换 step（给 Arco 100ms）
      setTimeout(() => {
        currentStep.value = N
        Message.success('完成')
      }, 100)
    } else {
      loading.value = false
      Message.error(res.data.msg)
    }
  } catch (e) {
    loading.value = false
    Message.error(e.message)
  }
  // ❌ 不要用 finally
}
```

#### 调试日志规范

```javascript
// 🔴 调试日志（修复后保留）
console.log('🔴 [DEBUG] someFunction 被调用')
console.log('🔴 当前状态:', {
  currentStep: currentStep.value,
  loading: loading.value,
})
```

### 后端规范

- 严格遵循 PEP8 规范
- 所有函数必须加类型提示
- 核心逻辑必须加注释

---

## 重要文档

| 文档 | 路径 |
|------|------|
| 重构方案 | `frontend/重构方案.md` |
| TRAE 任务单 | `frontend/TRAE 任务单_Frontend_Fix.md` |
| 项目状态报告 | `frontend/项目状态报告.md` |

---

**最后更新：** 2026-07-15  
**维护人：** 战舰 / TRAE
